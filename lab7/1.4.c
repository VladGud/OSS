#include <stdio.h>
#include <stdlib.h>
extern char **environ;

int main(int argc, char *argv[]) {
    char **ptr;
    int k = atoi(argv[i])
    for (ptr = environ; *ptr != NULL && ptr - environ < k; ptr++) {
        printf("%s\n", *ptr);
    }
    
    return 0;
}